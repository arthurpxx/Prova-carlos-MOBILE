para fazer rodar mobile pegue o ip local(pq quando estava tentando fazer rodar no celular tinha que usar o ip local da maquina)

1- Para criar as tabelas do banco tem que dar migrations (npm run migration)

2- Para rodar o back (npm run dev)

3- Para rodar o front (npm star)